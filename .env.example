# Example environment variables for Taskify
# Copy this file to .env and fill sensible values for your environment.

# Application
SECRET_KEY="change_me_generate_a_strong_one"
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# PostgreSQL (used by SQLAlchemy)
# Example: postgresql+psycopg2://<user>:<password>@<host>:<port>/<db>
DATABASE_URL="postgresql+psycopg2://postgres:password@localhost:5432/taskify"

# Redis for Celery broker/backend
REDIS_URL="redis://localhost:6379/0"
CELERY_BACKEND="redis://localhost:6379/1"

# Optional (docker-compose uses these)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=taskify

# Notes:
# - Generate a strong SECRET_KEY: `python -c "import secrets;print(secrets.token_urlsafe(48))"`
# - Never commit a real secret to source control; keep .env out of git.
